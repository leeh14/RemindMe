package teamspoiler.renameme;

import teamspoiler.renameme.DatabaseHelperClass;

/**
 * Created by hirats on 4/18/2016.
 */
public class MergeActivity {
    DatabaseHelperClass db;
    public void mergeActivities(int categoryID1, int categoryID2) {
        /*db = DatabaseHelperClass.getInstance();
        Category c1 = db.getCategory(categoryID1); Category c2 = db.getCategory(categoryID2);
        Category c = new Category(string name);
        db.addCategory(c);

        for (Item i : db.getItems(categoryID1))
            i.setCategoryID(c.getID());
            db.updateItem(i);
        for (Item i : db.getItems(categoryID2))
            i.setCategoryID(c.getID());
            db.updateItem(i);*/
    }
}

